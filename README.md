There are projects in the code

1.experiment_bundle  is tradition method to edit(attack) the watermarked image and test models
2.low-frequency filtering attack is the robustness test we designed based on low-frequency filteringThe robustness testing method we designed based on low-frequency filtering
3.vine-base is how we build vine-base model
